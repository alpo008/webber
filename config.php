<?php/*function __autoload($classname){    var_dump($classname);	switch($classname[0])	{		case 'C':			if (file_exists("c/$classname.php")) {				include_once("c/$classname.php");			}			break;		case 'M':			if (file_exists("m/$classname.php")) {				include_once("m/$classname.php");			}			break;	}}*/function __autoload($className){    $className = ltrim($className, '\\');    $fileName  = '';    $namespace = '';    if ($lastNsPos = strrpos($className, '\\')) {        $namespace = substr($className, 0, $lastNsPos);        $className = substr($className, $lastNsPos + 1);        $fileName  = str_replace('\\', DIRECTORY_SEPARATOR, $namespace) . DIRECTORY_SEPARATOR . $className;    }    $fileName .= '.php';    require $fileName;}define('BASE_URL', '/');/** * Данные для подключения к БД */define('MYSQL_SERVER', 'localhost');define('MYSQL_USER', 'root');define('MYSQL_PASSWORD', 'root');define('MYSQL_DB', 'webberry');/** * Размеры рабочего поля */define('BG_SIDE', 500);define('BG_BORDER', '2px solid #000');define('BG_COLOR', '#ccc');/** * Предельно допустимые параметры кругов */define ('RADIUS_MIN', 10);define ('RADIUS_MAX', 40);define ('COLOR_MAX', 16777215);